# Treat all files as text with Unix-style line ends by default
* text eol=lf

# Treat .bat files as text with Windows-style line ends by default
*.bat text eol=crlf

# Treat .cmd files as text with Windows-style line ends by default
*.cmd text eol=crlf
